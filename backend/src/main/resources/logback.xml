<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] traceId=%X{traceId} %-5level %logger{50} - %msg%n%ex{full}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 针对 com.freshmart.irs 包的日志级别配置 -->
    <logger name="com.freshmart.irs" level="DEBUG" additivity="true"/>

    <!-- 全局默认日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
